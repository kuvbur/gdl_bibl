call "Элемент" parameters all
values "gs_detlevel_3D" `Детальный`,`Простой`,`Откл.`

if gs_cont_pen=0 then parameters gs_cont_pen=4
if gs_fill_pen=0 then parameters gs_fill_pen=19
if gs_axis_pen=0 then parameters gs_axis_pen=64

If gs_resol<3 then parameters gs_resol=3
if gs_detlevel_3D=`Простой` then lock "gs_resol"

if gs_axis=0 then hideparameter "gs_axis_line"

parameters a=2*rac+rat,b=2*rac+rat
lock "a","b"

parameters dlin_st_model = (sqr(((2*3.14*rac)^2)+((zzyzx/(sang/360))^2)))*(sang/360)

parameters kol_vo_model = 1

if shag>0 then
	parameters sang=360*(ZZYZX/shag)
else
	parameters sang=360
endif
parameters ratt = r_arm
parameters ratp = r_arm
