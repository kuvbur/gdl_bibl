
EPS=0.00001

ELECTRIC = 1
GAS = 2

! --- Oven thickness
thi=0.002

! --- plate positions and size
dim rekx[2], reky[2]
dim xPlatePos[]
dim yPlatePos[]
dim plateSize[]

! --- switches
th=0.025
x=(aa-2*th)/3
y=(bb-2*th)/2

if CooktopType_m <= 4 then
	nPlates = 2
else
	if (CooktopType_m >= 5 and CooktopType_m <= 9) or CooktopType_m = 12 then
		nPlates = 4
	else
		! if CooktopType_m = 10 or CooktopType_m = 11 or CooktopType_m = 13
		nPlates = 5
	endif
endif

if cooktopType_m = 3 or cooktopType_m =4 or cooktopType_m = 8 or cooktopType_m =9 or cooktopType_m = 13 then
	b2DSymbol = 0
endif
