
EPS = 0.0001

! iHeadBoard:
HEADBOARD_BOTH	= 1
HEADBOARD_FRONT	= 2
HEADBOARD_REAR	= 3

! stFrameType
dim stFrameType[2]
	stFrameType[1] = `без Ящиков`
	stFrameType[2] = `с Ящиками`

! iFrameType
FRAME_LEG		= 1
FRAME_DRAWER	= 2

if GLOB_SCRIPT_TYPE = 7 | GLOB_SCRIPT_TYPE = 8 then goto "MasterEnd"

if bHeadboard then
	bFrontHeadBoard = (iHeadBoard = HEADBOARD_BOTH or iHeadBoard = HEADBOARD_FRONT)	! Both Sides or On Front Side
	bRearHeadBoard  = (iHeadBoard = HEADBOARD_BOTH or iHeadBoard = HEADBOARD_REAR)	! Both Sides or On Rear Side
	if not(bFrontHeadBoard) then
		hbh1 = 0
	endif
	if not(bRearHeadBoard) then
		hbh2 = 0
	endif
endif

if iBedLegType = 0 or iBedType = 1 then		!no leg
	bLeg = 0
	leg_h = 0
else
	bLeg = 1
endif


"MasterEnd":

