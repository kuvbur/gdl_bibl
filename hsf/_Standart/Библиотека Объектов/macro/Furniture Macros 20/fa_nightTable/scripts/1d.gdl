
! const values for parameter: iNightTableDrawerPos
DRAWERPOS_NONE = 0
DRAWERPOS_DOWN = 1
DRAWERPOS_UP   = 2

! Night Table 01 - 02 macro

EPS = 0.0001

IF not(bCornice) then
	tbt=0
	tboh=0
ENDIF

IF not(bBase) THEN
	leg_h=0
ENDIF

IF tbt=0 THEN
	tboh=0
ENDIF

IF gs_detlevel_3D_m=1 or gs_door_type_m=8 THEN
	bKnob = 0
ENDIF

! LOCK PARAMETERS
if bKnob then
	if gs_knob_type_m=1 or gs_knob_type_m=2 or gs_knob_type_m=3 then lock "knob_size"
endif

