
x1=h_1*SIN(cut1)
x2=h_2*SIN(cut2)
y1=h_1*COS(cut1)
y2=h_2*COS(cut2)

x0=b-x1+x2
y0=y2-y1

IF ABS(y0-0)>0.00001 THEN
	ang=ATN(x0/y0)
ELSE
	ang=90
ENDIF


IF ang>=0 THEN
	y11=((dp)/(COS(cut1+cutp1+90-ABS(ang))))*COS(cut1+cutp1)
	x11=((dp)/(COS(cut1+cutp1+90-ABS(ang))))*SIN(cut1+cutp1)
	y22=((dp)/(COS(cut2+cutp2+90-ABS(ang))))*COS(cut2+cutp2)
	x22=((dp)/(COS(cut2+cutp2+90-ABS(ang))))*SIN(cut2+cutp2)
ENDIF
IF ang<0 THEN
	y11=((dp)/(COS(cut1+cutp1-90+ABS(ang))))*COS(cut1+cutp1)
	x11=((dp)/(COS(cut1+cutp1-90+ABS(ang))))*SIN(cut1+cutp1)
	y22=((dp)/(COS(cut2+cutp2-90+ABS(ang))))*COS(cut2+cutp2)
	x22=((dp)/(COS(cut2+cutp2-90+ABS(ang))))*SIN(cut2+cutp2)
ENDIF
