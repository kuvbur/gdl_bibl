
if gs_detlevel_3D_m = DLEV3D_OFF then
	for i=1 to nsp
		n = get(1)
	next i

	end
endif


resol gs_resol
pen gs_cont_pen
material gs_slab_mat
if not(gs_shadow) then shadow off

addy B/2
rotz 90
rotx 90
prism_ NSP/3, A, get(NSP)

if bTopLayer then
	prism_ 4, a,
		-B/2, ZZYZX + 0.001,		8,
		-B/2, ZZYZX + slab_top_th,	13,
		 B/2, ZZYZX + slab_top_th,	8,
		 B/2, ZZYZX + 0.001,		13
endif
del 3

