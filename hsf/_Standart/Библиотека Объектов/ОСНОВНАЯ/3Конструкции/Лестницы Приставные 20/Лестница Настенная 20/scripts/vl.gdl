
call "FM_types" parameters all

values{2} "iLadderType" 	LADDERTYPE_FLATBAR, `Плоский Брусок`,
							LADDERTYPE_LSTEEL, `Г-образные Кронштейны`,
							LADDERTYPE_CONCRETE, `В Стене`
VALUES "b" 7"

VALUES "st" RANGE (0,]
VALUES "rw" RANGE (0,]
values "RungDist" RANGE (rw,)

LOCK "B"
IF iLadderType <> LADDERTYPE_CONCRETE THEN LOCK "st","dash_line"

if gs_cont_pen=0 then parameters gs_cont_pen=2
if gs_fill_pen=0 then parameters gs_fill_pen=91

values{2} "gs_detlevel_3D_m" 	DLEV3D_DETAILED, `Детальный`,
								DLEV3D_OFF, `Откл.`
VALUES "gs_resol" RANGE [3,)

VALUES "gs_ui_current_page" 1

if GLOB_MODPAR_NAME = "temp_RR" then
	PARAMETERS A = temp_RR*2
else
	PARAMETERS temp_RR = A/2
endif

ac_bottomlevel = 0
ac_toplevel = ZZYZX + EPS

parameters ac_toplevel = ac_toplevel, ac_bottomlevel = ac_bottomlevel

