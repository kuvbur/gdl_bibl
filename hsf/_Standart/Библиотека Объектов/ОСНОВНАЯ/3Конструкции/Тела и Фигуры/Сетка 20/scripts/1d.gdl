
dim stStyles[2]
	stStyles[1] = `Горизонтальная`
	stStyles[2] = `Вертикальная`

dim st_detail3D[2]
	st_detail3D[1] = `Детальный`
	st_detail3D[2] = `Откл.`

!--- minimal distance between mullions ---

minmdst = 0.15 + thickFrame

u = (xCoordEdges[2] - xCoordEdges[1] - (iNumFramesA-2)*thickFrame - 2*thickEdge) / (iNumFramesA-1)

if iStyle = 1 then
	iNumFramesB = iNumFramesB
	w = (yCoordEdges[2] - yCoordEdges[1] - (iNumFramesB-2)*thickFrame - 2*thickEdge) / (iNumFramesB-1)
else
	iNumFramesB = mVert
	w = (zCoordEdges[2] - zCoordEdges[1] - (iNumFramesB-2)*thickFrame - 2*thickEdge) / (iNumFramesB-1)
endif

if iNumFramesA = 1 or iNumFramesB = 1 then end
