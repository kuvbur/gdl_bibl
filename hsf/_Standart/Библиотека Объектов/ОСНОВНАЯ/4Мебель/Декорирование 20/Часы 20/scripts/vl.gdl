
call "FM_types" parameters all

!Hide all parameters from UI
hideparameter all  "A", "B", "ZZYZX"

values "hrs" RANGE [0, 23]
values "mnt" RANGE [0, 59]

parameters zzyzx = a
lock "zzyzx"

values{2} "gs_detlevel_3D_m" 	DLEV3D_DETAILED, `Детальный`,
								DLEV3D_SIMPLE, `Простой`,
								DLEV3D_OFF, `Откл.`
values "gs_resol" range [3,)

if gs_cont_pen = 0 then parameters gs_cont_pen=7
if gs_fill_pen = 0 then parameters gs_fill_pen=91

if gs_detlevel_3D_m = DLEV3D_SIMPLE then hideparameter "gs_resol"
if gs_detlevel_3D_m = DLEV3D_OFF then hideparameter "gs_shadow", "gs_resol"

if not(numb) then hideparameter "fontType"

parameters gs_font_size= abs(gs_font_size)
values "gs_font_size" range (0,]

