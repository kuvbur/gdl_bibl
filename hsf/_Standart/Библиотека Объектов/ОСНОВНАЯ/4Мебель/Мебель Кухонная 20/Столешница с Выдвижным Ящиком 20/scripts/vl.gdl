


if MSFront < 0 then
	MSFront = 0
	parameters MSFront = MSFront
endif

PARAMETERS door_w=a
values "A" range (0, )
values "B" range (0, )
values "bs_h"		range [0,)
if gs_count_type_m <> 0 then
	values "count_th"	range (0,)
endif
values "front_cab"	range [0,)

! --- LOCK PARAMETERS ---
if not(bDrawer) then
	LOCK "gs_bevel", "gs_door_type"
	hideparameter "gs_door_type"
endif

CALL "Kitchen Cabinets UI macro" parameters all \
									pageIds = pageIds,
									CornCabType = 0,
									bEdgeBevel = 0,
									CabinetType = 0,
									bToe = 0,
									bCounterOverhang = 1,
									bMirror = 0,

									bCabSymbol = 0,
									bPossibleSinknTap = 0,
									bApplianceEdit = 0,
									bSymbLineEdit = 0,
									bMinSpaceEdit = 1,
									bShadowEdit = 1,
									bHotspotEdit = 1,

									nDoors = 1,

									bEnableBackSplash	= 1,
									bEnableDoorpanel	= 1,
									bEnableEndpanel		= 0,
									bDoubleDoor			= 0,
									bEnableKnobPlacing	= 0,
									bEnableShelf		= 0,
									bEnableSink			= 0,
									isTallCabinet		= 0,

									bKnobParams = 1

if gs_door_type_m < EPS then
	lock "gs_knob_type", "knob_size", "fr_th1", "gs_bevel", "doorframe_w"
	hideparameter "gs_knob_type", "fr_th1", "gs_bevel", "doorframe_w"
endif

put 1, 2, 3, 4

if bDrawer then
	put 5
endif

values "gs_ui_current_page" get(nsp)

