
values "gs_ui_current_page" 1, 2

values "nColors" RANGE [1, )

values "nFillsInARow" RANGE [1, )

values{2} "iPenColorSymbStyle" 	SYMBOL_STYLE_1, `Стиль 1`,
								SYMBOL_STYLE_2, `Стиль 2`



values "font_size" range (0,]

parameters FE = 0
FE = 0

if BOL then
parameters FE = FE+1 :FE = FE+1 : endif
if ITA then
parameters FE = FE+2 :FE = FE+2 :endif
if UND then
parameters FE = FE+4 :FE = FE+4 :endif
